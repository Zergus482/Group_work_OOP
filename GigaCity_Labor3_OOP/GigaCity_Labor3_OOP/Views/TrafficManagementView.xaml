<UserControl x:Class="GigaCity_Labor3_OOP.Views.TrafficManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <TextBlock Grid.Row="0" Text="Управление трафиком" 
                   FontSize="20" 
                   FontWeight="Bold" 
                   HorizontalAlignment="Center" 
                   Margin="10"/>

        <!-- Панель управления -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
            <Button Content="Запустить симуляцию" 
                    Command="{Binding StartSimulationCommand}"
                    Margin="5"/>
            <Button Content="Остановить симуляцию" 
                    Command="{Binding StopSimulationCommand}"
                    Margin="5"/>
            <TextBlock Text="Скорость симуляции:" VerticalAlignment="Center" Margin="5"/>
            <Slider Value="{Binding SimulationSpeed}" Minimum="1" Maximum="10" Width="100" VerticalAlignment="Center" Margin="5"/>
        </StackPanel>

        <!-- Списки дорог и транспортных средств -->
        <TabControl Grid.Row="2" Margin="10">
            <TabItem Header="Дороги">
                <ListView ItemsSource="{Binding Roads}">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="ID" DisplayMemberBinding="{Binding X}" Width="50"/>
                            <GridViewColumn Header="X" DisplayMemberBinding="{Binding X}" Width="50"/>
                            <GridViewColumn Header="Y" DisplayMemberBinding="{Binding Y}" Width="50"/>
                            <GridViewColumn Header="Скорость" DisplayMemberBinding="{Binding SpeedLimit}" Width="100"/>
                            <GridViewColumn Header="Загруженность" DisplayMemberBinding="{Binding TrafficLevel}" Width="100"/>
                        </GridView>
                    </ListView.View>
                </ListView>
            </TabItem>
            <TabItem Header="Транспортные средства">
                <ListView ItemsSource="{Binding Vehicles}">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="ID" DisplayMemberBinding="{Binding Id}" Width="50"/>
                            <GridViewColumn Header="Тип" DisplayMemberBinding="{Binding Type}" Width="120"/>
                            <GridViewColumn Header="Текущая скорость" DisplayMemberBinding="{Binding CurrentSpeed}" Width="120"/>
                            <GridViewColumn Header="Движется" DisplayMemberBinding="{Binding IsMoving}" Width="80"/>
                        </GridView>
                    </ListView.View>
                </ListView>
            </TabItem>
        </TabControl>

        <!-- Панель добавления -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
            <TextBlock Text="Добавить транспорт:" VerticalAlignment="Center" Margin="5"/>
            <ComboBox Name="VehicleTypeComboBox" Width="120" Margin="5">
                <ComboBoxItem Content="Легковой автомобиль" Tag="Car"/>
                <ComboBoxItem Content="Автобус" Tag="Bus"/>
                <ComboBoxItem Content="Грузовик" Tag="Truck"/>
                <ComboBoxItem Content="Такси" Tag="Taxi"/>
                <ComboBoxItem Content="Скорая помощь" Tag="Emergency"/>
                <ComboBoxItem Content="Доставка" Tag="Delivery"/>
            </ComboBox>
            <Button Content="Добавить" 
                    Command="{Binding AddVehicleCommand}" 
                    CommandParameter="{Binding SelectedItem.Tag, ElementName=VehicleTypeComboBox}" 
                    Margin="5"/>
        </StackPanel>
    </Grid>
</UserControl>