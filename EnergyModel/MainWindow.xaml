<Window x:Class="EnergyModel.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Энергетическая система" Height="700" Width="900" WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
            <TextBlock VerticalAlignment="Center" Margin="12,0,0,0" x:Name="StatusText">Инициализация...</TextBlock>
        </StackPanel>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <ListView x:Name="CellsList" Grid.Column="0">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="X" Width="50" DisplayMemberBinding="{Binding X}"/>
                        <GridViewColumn Header="Y" Width="50" DisplayMemberBinding="{Binding Y}"/>
                        <GridViewColumn Header="Генерация" Width="90" DisplayMemberBinding="{Binding Generation}"/>
                        <GridViewColumn Header="Потребление" Width="90" DisplayMemberBinding="{Binding Consumption}"/>
                        <GridViewColumn Header="Баланс" Width="90" DisplayMemberBinding="{Binding Net}"/>
                        <GridViewColumn Header="Статус" Width="120" DisplayMemberBinding="{Binding Status}"/>
                    </GridView>
                </ListView.View>
            </ListView>

            <StackPanel Grid.Column="1" Margin="8,0,0,0">
                <TextBlock FontWeight="Bold" Text="Сводка" Margin="0,0,0,8"/>
                <TextBlock x:Name="TotalSupplyText" Text="Общая генерация: 0 МВт" Margin="0,0,4,8"/>
                <TextBlock x:Name="TotalConsumptionText" Text="Общее потребление: 0 МВт" Margin="0,0,0,8"/>
                <TextBlock x:Name="DeficitText" Text="Ячеек с дефицитом: 0" Margin="0,0,0,8"/>
                <TextBlock x:Name="SurplusText" Text="Ячеек с избытком: 0" Margin="0,0,0,8"/>
                <Separator Margin="0,8"/>
                <TextBlock FontWeight="Bold" Text="Статусы энергии" Margin="0,8,0,4"/>
                <TextBlock Text="• Критический: Дефицит &lt; -0.5 МВт" FontSize="10" Margin="0,2"/>
                <TextBlock Text="• Дефицит: Баланс &lt; 0 МВт" FontSize="10" Margin="0,2"/>
                <TextBlock Text="• Норма: Сбалансированная энергия" FontSize="10" Margin="0,2"/>
                <TextBlock Text="• Избыток: Энергия &gt; 2 МВт" FontSize="10" Margin="0,2"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
