<Window x:Class="EnergyModel.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Energy Model" Height="600" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
            <Button Width="140" Margin="0,0,8,0" Click="StartSimulation_Click">Start Simulation</Button>
            <Button Width="140" Click="StopSimulation_Click">Stop Simulation</Button>
            <TextBlock VerticalAlignment="Center" Margin="12,0,0,0" x:Name="StatusText">Idle</TextBlock>
        </StackPanel>

        <ListView Grid.Row="1" x:Name="CellsList">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="X" Width="50" DisplayMemberBinding="{Binding X}"/>
                    <GridViewColumn Header="Y" Width="50" DisplayMemberBinding="{Binding Y}"/>
                    <GridViewColumn Header="PowerSupply" Width="120" DisplayMemberBinding="{Binding PowerSupply}"/>
                    <GridViewColumn Header="Consumption" Width="120" DisplayMemberBinding="{Binding Consumption}"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Window>
